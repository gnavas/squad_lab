<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Index</title>
  <!-- Latest compiled and minified CSS -->
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.1/css/bootstrap.min.css">
</head>
<body>
  <table class ="table">
  <tr>
    <th>Squad ID</th>
    <th>Squad name</th>
    <th>Mascot</th>
    <th>Get more info</th>
  </tr>
 
  <% @squads.each do |x| %>
  <tr>
    <td><%= x["squad_id"] %></td>
    <td><%= x["squad_name"] %></td>
    <td><%= x["mascot"] %></td>
    <td> <form action='/squads/<%= x["squad_id"] %>' method='get' >
  <button type="submit" class="btn btn-default btn-xs"><%=x["squad_name"]%>_Info</button>
</form></td>
  </tr>
    <%end%>

</table>
<div>
   <form action='/squads/new' method='get' >
  <button type="submit" class="btn btn-default">Add Team</button>
</form>
</div>
</body>
</html>
